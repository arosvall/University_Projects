This assignment was a set of simple tasks involving C properties
t1 -> using character values to print a simple message
t2 -> Implementing a swap of pointers
t3 -> Removing whitespaces from a string
t4 -> Removing a character from a string and returning a substring of all chars after the removed one e.g. original -> inal if g is removed
t5 -> Trimming all the whitespace from before a string and after a string